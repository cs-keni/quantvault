# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Caching / task queue
redis==5.2.1
celery[redis]==5.4.0

# Auth
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
# Pinned <4.1: passlib 1.7.4's bcrypt self-test crashes on bcrypt>=4.1, which
# raises ValueError instead of silently truncating secrets >72 bytes — passlib
# is unmaintained (last release 2020) and will never ship a fix for this.
bcrypt==4.0.1
python-multipart==0.0.20

# Financial data & math
yfinance==0.2.51
pandas-datareader==0.10.0
numpy==2.2.1
pandas==2.2.3
scipy==1.14.1
